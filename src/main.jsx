import React from 'react';
import ReactDOM from 'react-dom/client';
import CubeWorld from '../sim.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CubeWorld />
  </React.StrictMode>
);
